s1={1,2,3,4}
s2={3,4,5,6}
# #union
# print(s1.union(s2))
# #intersection
# print(s1.intersection(s2))
                                   #here we are subtracting s1 - s2 so the common elements become zero and the other elements
#difference(s1-s2)                 # left in the s1 are printed - {1,2}#
# print(s1.difference(s2))         # here we are subtracting s2 - s1 so the common elements become zero and the other elements
#difference(s2-s1)                 # left in the s2 are printed - {5,6}
# print(s2.difference(s1))

